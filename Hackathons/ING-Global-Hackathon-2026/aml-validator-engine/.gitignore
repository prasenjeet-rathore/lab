# --- Python ---
.venv/
__pycache__/
*.py[cod]
*$py.class

# --- Java / Maven ---
target/
*.class
*.jar
!01_data_engine/amlsim/jars/mason.20.jar  # Keep this as it's a hard-to-find dependency

# --- Simulation Outputs & Temp Files ---
# We usually ignore huge generated datasets, but since it is only 3.1MB, we will keep it
# If it gets huge (>50MB), uncomment the lines below:
# 01_data_engine/amlsim/outputs/aml_hackathon_v1/
# 01_data_engine/amlsim/tmp/

# --- OS / IDE ---
.DS_Store
.vscode/
.idea/